import DeleteAccount from "@components/editUser/DeleteAccount";

export default function EditUserPage() {
  return (
    <section>
      유저의 이미지와 프로필을 수정할 수 있는 계정 설정 페이지
      <DeleteAccount />
    </section>
  );
}
